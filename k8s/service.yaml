apiVersion: v1
kind: Service
metadata:
  name: amazon-comp-tracker
  namespace: default
spec:
  selector:
    app: amazon-comp-tracker
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP
